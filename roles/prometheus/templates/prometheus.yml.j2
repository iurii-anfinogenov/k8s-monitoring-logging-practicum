global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:{{ prometheus_web_port }}"]

  - job_name: "node_exporter"
    static_configs:
      - targets: ["localhost:{{ node_exporter_port }}"]
